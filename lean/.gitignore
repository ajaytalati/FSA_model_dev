# Lean / Lake build artefacts and dependency caches.
# Mathlib cache alone is several GB; everything in .lake/ is reproducible
# from `lake update` + `lake exe cache get`.
.lake/

# Python cache from python_bridge/.
__pycache__/
*.pyc
